using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this appears in the bottom right corner of the screen in a tutorial. It's context sensitive, so it will display different
//information depending on w
public class HelpWindow : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
